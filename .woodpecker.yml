clone:
  git:
    image: docker.io/woodpeckerci/plugin-git:2.9.2
    settings:
      lfs: false

steps:
  - name: governed-release
    image: bash
    commands:
      - scripts/ci-governed-release
    when:
      - event: push
